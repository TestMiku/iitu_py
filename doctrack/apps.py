from django.apps import AppConfig


class DoctrackConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'doctrack'
    verbose_name = '0 ДОКТРЕКТ'
    verbose_name_plural = '0 ДОКТРЕКТ элементы'

